# Train Scheduler

This is webpage application that allows a user to create a train schedule by taking input, saving it to Firebase and then pulling it in to display.

The program was created using HTML, CSS (inline), Bootstrap, JavaScript, JQuery, AJAX and Firebase.
